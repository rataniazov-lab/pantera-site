import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ["Manrope", "system-ui", "-apple-system", "sans-serif"],
      },
      colors: {
        dark: "#0d1b2a",
        dark2: "#1a2b3c",
        accent: "#ff6b35",
        accent2: "#ffd166",
      },
      animation: {
        fireGlow: "fireGlow 2s ease-in-out infinite",
        scrollReviews: "scrollReviews 38s linear infinite",
      },
      keyframes: {
        fireGlow: {
          "0%, 100%": { boxShadow: "0 0 10px 2px rgba(255,100,0,0.55)" },
          "50%": { boxShadow: "0 0 22px 6px rgba(255,80,0,0.85), 0 0 40px 8px rgba(255,160,0,0.4)" },
        },
        scrollReviews: {
          "0%": { transform: "translateX(0)" },
          "100%": { transform: "translateX(-50%)" },
        },
      },
    },
  },
  plugins: [],
};

export default config;

content: [
  "./pages/**/*.{js,ts,jsx,tsx}",
  "./components/**/*.{js,ts,jsx,tsx}",
  "./app/**/*.{js,ts,jsx,tsx}",
],